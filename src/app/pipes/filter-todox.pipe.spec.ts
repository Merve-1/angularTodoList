import { FilterTodoxPipe } from './filter-todox.pipe';

describe('FilterTodoxPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterTodoxPipe();
    expect(pipe).toBeTruthy();
  });
});
